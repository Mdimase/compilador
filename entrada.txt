{
variable d is boolean = false;
variable entero? is integer = 12;
variable hj is integer = 1+65;
variable rt is float=5.56;
variable bv is boolean = rt == 5;
variable fghjl is boolean = d or true;
variable jk is float;
variable df is integer = 1 + rt;

function prueba_Funcion (a is integer) return integer
begin
    if entero? >= 10 then
        begin
            if hj == 66 then
                writeln(a/2);
        end
        else
            writeln("Estoy en el else");
    return a;
end;

function pepe (a is float) return integer
    begin
        salamin(a+8);
        variable aux is integer = 4000;
        writeln(aux+p);
            if aux > 10-8 then
                begin
                    variable aba is boolean;
                end
            else
                salamin(a+5);
    end;

function salamin(z is integer) return float
    begin
        while z < 12 do # las condiciones siempre evaluan que su resultado sea un bool
            begin
                writeln(z);
                z=z+1;
            end;
            # como no tiene un return, le agregamos uno implicitamente que retorna un valor por defecto segun el tipo de retorno de la funcion.
    end;

variable p is integer = 15 + hj;
variable xz is integer = 100;
variable poyo is float = rt - jk;
variable b is boolean;
variable nm is integer = 560;
variable rty is integer;
variable l is boolean=not d;
variable kl is boolean = hj != df;
variable po is float = 12 + nm;

function hola(queso is integer,jamon is float,skt is boolean=true) return boolean
        begin
        skt=false;
        return skt;
        end;
}
main is
{
variable j is integer = 50;
j = j + 120-20;
variable asd is float = -1.23;
}
variable zz is boolean;
{
variable int is integer = j + 500;
int = int + 1;

for t8 from 6 to 1 by 2 do
    begin
        writeln("daleee");
        writeln(t8);
    end;

when j
        is > 100.23 + j then writeln("j>100.23+j")
        is < 253.98 then writeln("j<253.98")
        else writeln("sino");

if j == 56 and int < 100
    then writeln("soy t") else writeln("soy f");

}
variable ju is integer = 50;

while ju != 55 do
    begin
        zz = 5 < ju and zz ;
    end;
{
writeln(hola(10,50));
variable vbn is boolean = read_boolean();
writeln(vbn);
variable aw is integer = read_integer();
prueba_Funcion(aw);
#salamin(1);
#pepe(1.5);
}
end.
