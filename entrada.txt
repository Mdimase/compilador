## Testeando compilador de Dimase Matias e Isea Maximiliano ##
#variable a is float = 1 + pepe();

{function prueba_Funcion (a is float, b is integer = 21, sdf is boolean = false) return integer
begin
    if entero? >= 10 then
        begin
            a=5;
            variable jk is integer=14;
            pepe();
        end
        else
            writeln("Estoy en el else");
    return a+b;
end;}

function prueba_Funciones (c is integer, j is boolean) return float
    begin
        c=c+2;
        if j == true then c=c+5;
        return c;
    end;

function pepe (a is float) return boolean
    begin
        variable b is boolean;
            if b == false then a=2 else a=4;
        return b;
    end;

variable c is integer;
#variable cd is float = 1 + 1 / random();


main is
{
if a==1 then
    begin
        while a==1 do
            begin
                if a>3 then a=2;
                #variable x is boolean;
                variable lk is float;
            end;
        if b==4 then b=1;
    end;
}

variable x is boolean;
{
variable b is boolean=true;
variable fg is integer;
variable g is integer;
variable f is float;
#x = x == b;
#x = -x;
#g = not x;
#g = g and g;
#x = x + x;
g = g + prueba_Funciones(c);}
variable j is integer;
end.